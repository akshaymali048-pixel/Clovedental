# Assumptions

## Service content

Service content is maintained by the developer via MDX files in `content/services/`.

**Trigger for adding a CMS:** the clinic owner requests content changes themselves more than twice after launch.
